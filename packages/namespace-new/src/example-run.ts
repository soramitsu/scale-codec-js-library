import { str } from './example';

str.encode('afjslsl');

// const person: Example.Person.Decoded = {
//     name: 'hello',
//     age: 41,
// };

// const encoded = Example.Person.encode(person);
// console.log(encoded);
// console.log(Example.Person.decode(encoded));
